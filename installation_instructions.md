# Installation Instructions

## Windows
1. Install Python 3.10+
2. Run: pip install -r requirements.txt
3. Run: python process_monitor.py

## Linux / Mac
1. Install Python 3
2. Install psutil via: sudo apt install python3-psutil
3. pip install -r requirements.txt
4. python3 process_monitor.py
